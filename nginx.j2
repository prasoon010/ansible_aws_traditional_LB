upstream backend {
      server {{ backend1_ip }};
      server {{ backend2_ip }};

   }


   server {
      listen 80;
      server_name {{domain}} www.{{domain}};
      location / {
          proxy_pass http://backend;
      }
   }
